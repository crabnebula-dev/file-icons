{
  "name": "file-icons",
  "private": true,
  "version": "0.1.0",
  "description": "Solid CSR Template",
  "license": "PolyForm-Noncommercial-1.0.0",
  "author": "CrabNebula <hello@crabnebula.dev>",
  "type": "module",
  "main": "dist/file_icons.js",
  "types": "dist/file_icons.d.ts",
  "files": [
    "dist",
    "icons"
  ],
  "scripts": {
    "gen": "tsup scripts/gen.ts --out-dir scripts --format esm && node scripts/gen.js",
    "build": "wasm-pack build . --release --out-dir=dist -- -Z build-std=core,std,alloc,panic_abort -Z build-std-features=panic_immediate_abort"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "wasm-pack": "^0.12.1"
  }
}
